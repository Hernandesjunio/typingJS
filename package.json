{
  "name": "typingjs",
  "version": "1.0.0",
  "description": "Biblioteca para simular digitação com cursor piscando",
  "main": "typing.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "esbuild typing.js --minify --outfile=dist/typing.min.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.6"
  }
}
